---
title: "GameHubFactory"
description: "Deterministic CREATE2 deployer for per-user GameHub accounts, plus single-signature onboarding and refill flows."
---

<Note>
  Auto-generated from Solidity NatSpec via `forge doc`. Hand edits here will be overwritten.
  Run `npm run sync-contracts` from `docs-site/` to regenerate.
</Note>

<Info>
  **Predeploy address:** `0x4200000000000000000000000000000000000000A0`
</Info>

Deterministically deploys one GameHubAccount per EOA via CREATE2.
Also provides an atomic deposit + session-registration flow so
first-time users complete onboarding in a single signature.
Designed to be a predeploy (address TBD).


## State Variables
### sessionKeys

```solidity
SessionKeyManager public immutable sessionKeys
```


### registry

```solidity
GameRegistry public immutable registry
```


## Functions
### constructor


```solidity
constructor(SessionKeyManager skm, GameRegistry reg) ;
```

### hubOf

Deterministic hub address for a given owner.


```solidity
function hubOf(address owner) public view returns (address);
```

### depositAndRegister

Deploy Hub if missing, then deposit + register a session key
in a single call. Intended as the one-signature onboarding tx.


```solidity
function depositAndRegister(address sessionKey, SessionKeyManager.Scope calldata scope)
    external
    payable
    returns (address hubAddr);
```

### depositAndRefill

Deposit + refill an existing session in a single signature.

msg.value is added to balances[game]; addedCap and newExpiry update
the session's on-chain scope. Requires the Hub to already exist and
the session to be active (not revoked).


```solidity
function depositAndRefill(address sessionKey, address game, uint256 addedCap, uint64 newExpiry)
    external
    payable
    returns (address hubAddr);
```

## Events
### HubDeployed

```solidity
event HubDeployed(address indexed owner, address hub);
```

