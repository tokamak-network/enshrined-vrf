{
  "name": "enshrined-vrf-docs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "mintlify dev",
    "broken-links": "mintlify broken-links",
    "sync-contracts": "node scripts/sync-contracts.mjs",
    "build": "npx mintlify export --output .build.zip && rm -rf out && mkdir out && cd out && unzip -oq ../.build.zip && cd .. && rm .build.zip",
    "build:full": "npm run sync-contracts && npm run build"
  },
  "devDependencies": {
    "mintlify": "latest"
  }
}
